spring:
  data:
    redis:
      host: localhost
      port: 6379

weather:
  search:
    scheduler:
      period: 1
      time-unit: MINUTES

apple:
  team-id: ${TEAM_ID}
  push:
    notification:
      app-id: ${APNS_APP_ID}
      key-id: ${APNS_KEY_ID}

logging:
  level:
    project.app.apns_server.modules : debug